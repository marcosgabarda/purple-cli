# purple-cli

CLI tool to retrieve information from Twitch.

## Settings

This project requires the following environment variables to work:

- `PURPLE_CLIENT_ID` Twitch application client id.
- `PURPLE_CLIENT_SECRET` Twitch application client secret.
